<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kolibreath.onit">
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name="com.example.kolibreath.onit.App"
        android:allowBackup="true"
        android:icon="@mipmap/icon192"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterActivity"/>
        <activity android:name=".OnitMainActivity"/>
        <activity android:name=".UserMainActivity"/>
        <activity android:name=".SearchforBBFActivity"/>
        <activity android:name=".CreateNewDongtai"/>
        <activity android:name=".SelectActivity"/>
        <activity android:name=".SelectAvatarActivity"/>
        <activity android:name=".Comment"/>
    </application>

</manifest>